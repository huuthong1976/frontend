{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,

  "homepage": "https://thoidaiso.info.vn",

  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },

  "dependencies": {
    "react-scripts": "5.0.1"
    /* thêm các deps khác của bạn ở đây, nhớ ngăn cách bằng dấu phẩy và KHÔNG để dấu phẩy sau phần tử cuối */
  },

  "eslintConfig": {
    "extends": ["react-app", "react-app/jest"]
  },

  "browserslist": {
    "production": [">0.2%", "not dead", "not op_mini all"],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}

